// External URLs the app links out to. PRIVACY_POLICY_URL points at the
// website's own /privacy page (web/src/app/privacy/page.tsx) so the legal
// text lives in exactly one place. Confirmed live and correct (fetched and
// verified it's actually the Borteh admin site, not a guess) — the owner has
// no plan to move this admin site off its Vercel URL to a custom domain.
export const PRIVACY_POLICY_URL = "https://borteh-sprays-admin.vercel.app/privacy";

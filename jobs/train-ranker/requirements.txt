# Phase 4 ranker trainer (activate at ~10k events). Python because LightGBM's LGBMRanker
# has no Node equivalent; this is the only Python in the stack and runs offline only.
psycopg[binary]>=3.1
lightgbm>=4.0
scikit-learn>=1.3
numpy>=1.24
